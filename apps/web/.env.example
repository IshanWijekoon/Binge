# Use same-origin proxy in local dev so auth cookies work in the browser.
NEXT_PUBLIC_API_BASE_URL=/api/v1

# Optional: override the backend target used by Next.js rewrites.
# API_PROXY_TARGET=http://localhost:8787
